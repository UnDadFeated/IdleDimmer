v1.4.0 — Bug audit sweep: group dim removed, hotkey config sync, wheel coordinates

• Removed all groupDim branching from toggle/drag/scroll/arrow handlers
  — per-monitor is now the only mode. Stale INI files can't re-enable it.
• Ctrl+Alt+D now updates m_config.dimmingEnabled so the DimmingEnabled
  checkbox stays in sync and saves/restores correctly.
• WM_MOUSEWHEEL screen→client coordinate bug fixed (slider hit-testing
  was using screen-space coordinates).
• Fixes 2 (auto-enable SetDimmingEnabled) and 4 (CoInitializeEx) from
  the audit report were already in place — verified.
